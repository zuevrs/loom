---
name: loom-verify
description: Two-axis review (Spec + Standards) of any diff. Usually invoked automatically by loom-implement before done; call directly for ad-hoc review of a branch, PR, or another agent's changes
---

Read and follow the `loom-verify` skill (`skills/loom-verify/SKILL.md`).

When called directly without an active issue: ask the user for the fixed point (branch, commit, or merge-base) and use the best available spec source (issue, PRD, PR description, or the user's stated intent).
