import { equal, deepStrictEqual } from "node:assert";import { createRequire } from "node:module";import { dirname,resolve } from "node:path";import { fileURLToPath } from "node:url";const root=resolve(dirname(fileURLToPath(import.meta.url)),"..");const require=createRequire(import.meta.url);const k=require(resolve(root,"hooks/story.cjs"));
const context=k.validateContextKnowledge({registeredRepositories:["api","web"],entries:[{name:"Order",kind:"Term",scope:[]},{name:"order-events",kind:"Contract",scope:["api","web"]}]});deepStrictEqual(context,{valid:true,contracts:["order-events"]});equal(k.validateContextKnowledge({registeredRepositories:["api"],entries:[{name:"Bad Name",kind:"Contract",scope:[]}]}).valid,false);equal(k.validateContextKnowledge({registeredRepositories:["api"],entries:[{name:"order-events",kind:"Contract",scope:["db"]}]}).valid,false);
equal(k.validateAdrScope({scope:"workspace-wide",registeredRepositories:["api"],contractNames:[]}).valid,true);equal(k.validateAdrScope({scope:{repositories:["api"],contracts:["order-events"]},registeredRepositories:["api"],contractNames:["order-events"]}).valid,true);equal(k.validateAdrScope({scope:{repositories:["api-id"],contracts:[]},registeredRepositories:["api"],contractNames:[]}).valid,false);equal(k.validateAdrScope({scope:{repositories:[],contracts:["missing"]},registeredRepositories:["api"],contractNames:["order-events"]}).valid,false);
equal(k.planAdrEvolution({applied:false,compatible:false,clarification:false}).action,"AMEND");equal(k.planAdrEvolution({applied:true,compatible:false,clarification:false}).action,"SUPERSEDE");equal(k.planAdrEvolution({applied:true,compatible:false,clarification:true}).action,"AMEND_NOTE");deepStrictEqual(k.planKnowledgeWrite({actor:"worker",changes:["ADR"]}),{valid:true,action:"DECISION_NEEDED",changes:[]});deepStrictEqual(k.planKnowledgeWrite({actor:"coordinator",changes:["ADR","CONTEXT","ADR"]}),{valid:true,action:"WRITE",changes:["ADR","CONTEXT"]});
console.log("scoped knowledge tests passed");
